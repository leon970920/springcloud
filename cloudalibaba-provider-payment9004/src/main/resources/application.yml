server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos服务地址

management:
  endpoints:
    web:
      exposure:
        include: "*" #监控节点全开